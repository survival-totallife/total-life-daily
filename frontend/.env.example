# Frontend Environment Variables
# Copy this file to .env.local for local development

# Backend API URL (include the port)
# For local dev: use localhost with your backend port
NEXT_PUBLIC_API_URL=http://localhost:8000

# For Docker: use the service name (uncomment the line below when using docker-compose)
# NEXT_PUBLIC_API_URL=http://backend:8000
