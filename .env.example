# Google Gemini API Key - required for the wellness bot
# you can get a gemini api key w/ totallife email at https://aistudio.google.com/apikey
GOOGLE_API_KEY=

# Optional: Gemini model to use (defaults to gemini-2.0-flash)
# Options: gemini-2.0-flash (fast/cheap), gemini-2.5-flash (latest), gemini-2.5-pro (more capable)
GEMINI_MODEL=gemini-2.0-flash

# Backend port (defaults to 8000)
BACKEND_PORT=8000

# Database URL
# For local development with SQLite (default):
# DATABASE_URL=sqlite:///./data/articles.db
# For Cloud SQL MySQL:
# DATABASE_URL=mysql+pymysql://USERNAME:PASSWORD@/DATABASE_NAME?unix_socket=/cloudsql/PROJECT_ID:REGION:INSTANCE_NAME
